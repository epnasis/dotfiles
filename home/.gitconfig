[alias]
  a = add
  s = status
  ds = diff --staged
  l = log --all --decorate --oneline --graph
  d = diff
  c = commit

[user]
  email = pawel@wenda.eu
  name = Pawel Wenda
	signingkey = /Users/epnasis/.ssh/git_signing_key.pub

[gpg]
  format = ssh

; [gpg "ssh"]
;   program = "/Applications/1Password.app/Contents/MacOS/op-ssh-sign"
	; allowedSignersFile = ~/.config/git/allowed_signers
	;
[commit]
  gpgsign = true


